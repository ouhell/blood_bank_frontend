import Particles from "@/components/Particles";
import React from "react";
import { motion as m } from "framer-motion";

const LandingPage = () => {
  return (
    <div className="h-screen -z-20 bg-green-300 relative">
      <Particles />
    </div>
  );
};

export default LandingPage;
